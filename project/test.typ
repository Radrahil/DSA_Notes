#import "template.typ": BookTemplate

#show: doc => BookTemplate(doc)


This is a paragraph with `inline raw text` that now has a background.
It also works with longer snippets like `code with spaces`.

#include "lectures/lec6.typ"

